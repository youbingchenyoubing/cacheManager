# ifndef PARSEHASH_H
# define PARSEHASH_H
# include <string>
# include <string.h>
using namespace std;
namespace cachemanager{
bool  parseHash(string rule,string & fileName,const string & hashName);
}
#endif